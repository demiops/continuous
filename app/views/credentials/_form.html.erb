<%= form_with(model: credential, local: true) do |form| %>
  <% if credential.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(credential.errors.count, "error") %> prohibited this credential from being saved:</h2>
      <ul>
      <% credential.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label :cloudkey, class: 'col-sm-2 col-form-label' %>
    <div class="col-sm-10">
    <%= form.text_field :cloudkey, class: 'form-control' %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label :cloudsecret, class: 'col-sm-2 col-form-label' %>
    <div class="col-sm-10">
    <%= form.text_field :cloudsecret, class: 'form-control' %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label :cloudtoken, class: 'col-sm-2 col-form-label' %>
    <div class="col-sm-10">
    <%= form.text_area :cloudtoken, class: 'form-control' %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label :cloudtype, class: 'col-sm-2 col-form-label' %>
    <div class="col-sm-10">
    <%= form.text_field :cloudtype, class: 'form-control' %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label :clouddata, class: 'col-sm-2 col-form-label' %>
    <div class="col-sm-10">
    <%= form.text_area :clouddata, class: 'form-control' %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit 'Submit', class: 'btn btn-primary' %>
  </div>
<% end %>
